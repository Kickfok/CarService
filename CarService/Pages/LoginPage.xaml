<Page x:Class="CarService.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CarService.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Авторизация">

    <Grid>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">


            <StackPanel Orientation="Horizontal" Width="218">
                <TextBlock Style="{StaticResource BlockCommon}" Text="Логин:"/>
                <Separator Width="20" Background="{x:Null}"/>
                <TextBox x:Name="TBoxLogin" Width="150"/>
            </StackPanel>

            <Separator Height="10" Background="{x:Null}"/>

            <StackPanel Orientation="Horizontal">
                <TextBlock Style="{StaticResource BlockCommon}" Text="Пароль:"/>
                <Separator Width="10" Background="{x:Null}"/>
                <PasswordBox x:Name="PBoxPassword" Width="150"/>
            </StackPanel>

            <Separator Height="15" Background="{x:Null}"/>

            <Button x:Name="BtnLogin" Content="Войти" HorizontalAlignment="Right" Click="BtnLogin_Click" Width="150" Style="{StaticResource ServiceButtons}"/>
        </StackPanel>

    </Grid>
</Page>
